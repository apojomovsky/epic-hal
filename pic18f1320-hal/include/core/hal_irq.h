/*
 * Family-neutral `EPIC_IRQ_*` interrupt-control contract. Each family
 * ships its own copy declaring the API against its own `PIC*_IRQn` enum.
 */

#ifndef EPIC_HAL_IRQ_H
#define EPIC_HAL_IRQ_H
#include "pic18_irq.h"
#endif /* EPIC_HAL_IRQ_H */
